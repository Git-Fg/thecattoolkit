---
description: "Shortcut: Manage Python environment, dependencies (uv), and code quality (ruff)."
argument-hint: "[install|add|check|format|run] <args>"
allowed-tools: [Skill(uv-ruff-python-tools)]
disable-model-invocation: true
---

Invoke the `uv-ruff-python-tools` skill. Pass the following arguments as context: "$ARGUMENTS"
